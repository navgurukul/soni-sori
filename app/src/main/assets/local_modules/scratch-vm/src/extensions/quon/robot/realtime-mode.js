const turnLeft = (args, util, parent) => {
    console.log(`From realtimeMode: ${args}`);
}

const turnRight = (args, util, parent) => {
    console.log(`From realtimeMode: ${args}`);
}

const moveStraight = (args, util, parent) => {
    console.log(`From realtimeMode: ${args}`);
}

const getAngle = (args, util, parent) => {
    console.log(`From realtimeMode: ${args}`);
}
module.exports = {
    turnLeft,
    turnRight,
    moveStraight,
    getAngle
};
