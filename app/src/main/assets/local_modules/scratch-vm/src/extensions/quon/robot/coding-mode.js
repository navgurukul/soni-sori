const turnRight  = (args, util) => {
    util.runtime.include.add(`#include <quon.h>\n`);
}

const turnLeft  = (args, util) => {
    util.runtime.include.add(`#include <quon.h>\n`);
}

const moveStraight  = (args, util) => {
    util.runtime.include.add(`#include <quon.h>\n`);
}

const getAngle  = (args, util) => {
    util.runtime.include.add(`#include <quon.h>\n`);
}

module.exports = {
    turnLeft,
    turnRight,
    moveStraight,
    getAngle
};
